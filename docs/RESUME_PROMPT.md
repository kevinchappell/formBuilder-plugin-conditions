# Resume prompt

The conditional visibility plugin work on branch `feat/conditional-visibility` is complete through the final whole-branch review; see `docs/HANDOFF.md` for status, decisions, and deferred findings. Remaining steps require separate authorization: merge, push, PR, or publish. Optional follow-ups: address the deferred minor list, amend the spec's hidden-source sentence, add `files`/`exports`/LICENSE and flip `private` before publishing, and inspect `examples/basic.html` in a real browser.
